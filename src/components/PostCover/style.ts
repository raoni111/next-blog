import styled from 'styled-components';
import theme from '../../styles/theme';

const Conteiner = styled.img`
  max-width: 100%;
  margin-bottom: ${theme.spacings.medium};
  width: 100%;
`;

export default Conteiner;
